import React from "react";
//const Specification = (props) => (

class Specification extends React.Component{
    render() {
        return (
            <div>
                <h2>Weather app - "here and now"</h2>
                <p>check the weather here and now!</p>
            </div>
        );
    }
}
export default Specification;
